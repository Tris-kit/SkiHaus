{
  "name": "skihaus-server",
  "version": "0.1.0",
  "private": true,
  "description": "SkiHaus backend — accounts, houses, ledger, votes, guest links (Next.js on Vercel).",
  "engines": { "node": "22.x" },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:web": "node scripts/build-web.mjs",
    "vercel-build": "node scripts/build-web.mjs && next build",
    "start": "next start",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "next": "^15.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.9.0"
  }
}
