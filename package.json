{
  "name": "skihaus-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Ski House monorepo — mobile/ (Expo app: iPhone + web) and server/ (Next.js: API, public guest/invite pages, and hosts the web app).",
  "scripts": {
    "ios": "npm --prefix mobile run ios",
    "web": "npm --prefix mobile run web",
    "server": "npm --prefix server run dev",
    "build": "npm --prefix server run vercel-build",
    "check": "npm --prefix mobile run check && npm --prefix server run check",
    "test": "npm --prefix mobile run test"
  }
}
